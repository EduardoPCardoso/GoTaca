package com.gotaca.gotaca;

public interface Stations {
    /*
    Station place(int id); //Devolve o posto dado o seu id.
    void create(Station station); //Adiciona o posto indicado.
    int newplace(); //Adiciona um posto em branco.
    void delete(int id); // Elimina o posto indicado;
    int sizeplace(); //Retorna o número de postos;
    void update(int id, Station station); //Substitui o posto.*/
    final static String TAG = "GoTaca";
    static GeoPoint actualPosition = new GeoPoint(0,0);
}
